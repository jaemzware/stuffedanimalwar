# Python dependencies for Pi Camera WebRTC Broadcaster

# WebRTC library for Python
aiortc>=1.6.0

# Socket.io client for signaling
python-socketio[asyncio_client]>=5.10.0

# DNS library (required by aioice for WebRTC)
dnspython>=2.4.0

# Video processing
av>=10.0.0
numpy>=1.24.0

# USB camera support (OpenCV)
# For Pi Zero 2W, use system opencv instead: sudo apt install python3-opencv
# opencv-python-headless>=4.8.0

# Raspberry Pi CSI camera support (Pi Camera Module)
# Install with: sudo apt install python3-picamera2
# picamera2
