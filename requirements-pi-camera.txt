# Python dependencies for Pi Camera WebRTC Broadcaster

# WebRTC library for Python
aiortc>=1.6.0

# Socket.io client for signaling
python-socketio[asyncio_client]>=5.10.0

# Video processing
av>=10.0.0
numpy>=1.24.0

# USB camera support (OpenCV)
# Uncomment if using USB camera:
opencv-python>=4.8.0

# Raspberry Pi CSI camera support (Pi Camera Module)
# Install with: pip install picamera2
# Note: picamera2 has system dependencies (libcamera-dev, python3-picamera2)
# Uncomment if using Pi Camera Module:
# picamera2
