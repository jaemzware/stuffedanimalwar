# Python dependencies for Pi Camera WebRTC Broadcaster

# WebRTC library for Python
aiortc>=1.6.0

# Socket.io client for signaling
python-socketio[asyncio_client]>=5.10.0

# Video processing
av>=10.0.0
numpy>=1.24.0

# Raspberry Pi camera support (only needed on Pi)
# Install with: pip install picamera2
# Note: picamera2 has system dependencies, see PI_CAMERA_SETUP.md
# picamera2

# Optional: for alternative video sources
# opencv-python>=4.8.0
